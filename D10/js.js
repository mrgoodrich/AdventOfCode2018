for (var ndx = 0; ndx < arr.length; ndx++) {
  var val = arr[ndx];

  console.log(val);
}

for (var ndx in arr) {
  var val = arr[ndx];

  console.log(val);
}


var obj = {};
obj[x] = v;
obj.x = v;

[1,2,3].map(v => {
  return v + 2;
}).forEach(v => {
  console.log(v);
});



